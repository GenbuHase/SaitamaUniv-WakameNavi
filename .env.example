# Upstash Redis (Vercel KV) — required in production for rate limiting / cache.
# Without these, the API returns 503 in production.
KV_REST_API_URL=https://your-upstash-redis.upstash.io
KV_REST_API_TOKEN=your-upstash-token
